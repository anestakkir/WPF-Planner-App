<Window x:Class="new_planner.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:new_planner.Views"
        mc:Ignorable="d"
        Title="Вход в систему" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        FontSize="14">
    <Grid Margin="15">
        <StackPanel VerticalAlignment="Center">
            <Label Content="Имя пользователя:"/>
            <TextBox Margin="0,0,0,10" Padding="5" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Content="Пароль:"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20" Padding="5"/>

            <Button Content="Войти" Padding="15,5" Margin="0,0,0,5" 
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox}"/>

            <Button Content="Зарегистрироваться" Padding="15,5" Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>