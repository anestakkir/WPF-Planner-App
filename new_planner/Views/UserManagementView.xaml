<Window x:Class="new_planner.Views.UserManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление пользователями" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding Users}" AutoGenerateColumns="False"
                  SelectedItem="{Binding SelectedUser}" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Username}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding RoleName}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сбросить пароль..." Padding="10,5" Margin="5
" Command="{Binding ResetPasswordCommand}"/>
            <Button Content="Удалить пользователя" Padding="10,5" Margin="5" Command="{Binding DeleteUserCommand}"/>
            <Button Content="Сохранить изменения" Padding="10,5" Margin="5" IsDefault="True" Command="{Binding SaveChangesCommand}"/>
        </StackPanel>
    </Grid>
</Window>